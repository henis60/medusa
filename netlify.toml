[build]
  command = "turbo run build --filter @dtc/storefront"
  publish = "apps/storefront/.next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

[[plugins]]
  package = "@netlify/plugin-nextjs"
